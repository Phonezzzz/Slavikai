# CLAUDE.md

Claude Code project entrypoint.

Canonical project instructions live in `AGENTS.md`.

Before any task:
1. Read `AGENTS.md`.
2. Follow the canonical rules from `AGENTS.md`.
3. If `CLAUDE.md` conflicts with `AGENTS.md`, `AGENTS.md` wins.

Do not duplicate project rules here.
