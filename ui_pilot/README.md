# UI-pilot (PR-0)

UI-pilot — это инженерный workbench, раздаётся из backend по адресу `/pilot/`.

## Сборка

```bash
npm install
npm run build
```

## Важно

`ui_pilot/dist/` коммитится намеренно для PR-0, чтобы backend мог раздавать статический UI без отдельного пайплайна.
