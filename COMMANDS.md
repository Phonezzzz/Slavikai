# 📜 Команды SlavikAI Core v0.5

**Основные:**
- `/fs list` — показать файлы песочницы
- `/fs read <файл>` — прочитать файл
- `/sh <команда>` — безопасная shell-команда
- `/web <url>` — получить web-страницу

**Память:**
- `/mem search <текст>` — поиск в долговременной памяти

**Векторная память:**
- `/vec search <текст>` — семантический поиск по embeddings

**Проекты:**
- `/project index <папка>` — проиндексировать все файлы `.py`, `.txt`, `.md`
- `/project find <текст>` — найти похожие участки кода
