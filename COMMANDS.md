# 📜 Команды SlavikAI Core

## Основные

- `/fs list [path]` — список файлов/папок в `sandbox/` (по умолчанию `.`)
- `/fs read <path>` — прочитать файл из `sandbox/`
- `/sh <command>` — безопасная shell-команда (whitelist + запреты `&&/||/;`, абсолютных путей и `..`)
- `/web <query>` — web search по запросу (может быть отключён safe-mode)
- `/project index [path]` — проиндексировать файлы в `sandbox/project/` (по умолчанию `.`)
- `/project find <query>` — поиск по векторному индексу (code+docs)

## Планирование

- `/plan <goal>` — построить и выполнить план (в текущей реализации — упрощённый путь)
- `/auto <goal>` — автозапуск подагентов (в текущей реализации — упрощённый путь)

## Картинки

- `/imggen <prompt>` — генерация изображения (если инструмент включён)
- `/imganalyze <path|base64>` — анализ изображения (если инструмент включён)

## Диагностика

- `/trace` — последние записи трейса
